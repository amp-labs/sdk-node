/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { readDeliverResults } from "../../funcs/readDeliverResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeliverResultsRequest$inboundSchema,
};

export const tool$readDeliverResults: ToolDefinition<typeof args> = {
  name: "read_deliver-results",
  description: `Deliver results

If a read action has delivery mode set to onRequest, then use this endpoint to request for more results. Learn more in [Delivery modes](https://docs.withampersand.com/define-integrations/read-actions#delivery-modes).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await readDeliverResults(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
