/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectsAndFieldsGetMetadataForInstallation } from "../../funcs/objectsAndFieldsGetMetadataForInstallation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetObjectMetadataForInstallationRequest$inboundSchema,
};

export const tool$objectsAndFieldsGetMetadataForInstallation: ToolDefinition<
  typeof args
> = {
  name: "objects-and-fields_get-metadata-for-installation",
  description: `Get object metadata for installation

Retrieves metadata about an object in a customer's SaaS instance, including its fields.
This endpoint requires that an Installation exists for the given groupRef.
It applies object mappings.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await objectsAndFieldsGetMetadataForInstallation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
