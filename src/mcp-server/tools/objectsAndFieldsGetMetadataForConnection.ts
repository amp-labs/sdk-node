/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectsAndFieldsGetMetadataForConnection } from "../../funcs/objectsAndFieldsGetMetadataForConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetObjectMetadataForConnectionRequest$inboundSchema,
};

export const tool$objectsAndFieldsGetMetadataForConnection: ToolDefinition<
  typeof args
> = {
  name: "objects-and-fields_get-metadata-for-connection",
  description: `Get object metadata for connection

Retrieves metadata about an object in a customer's SaaS instance, including its fields.
This endpoint only requires that a Connection exists for the given groupRef.
It does not apply any object mappings.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await objectsAndFieldsGetMetadataForConnection(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
